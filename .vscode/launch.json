{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Indexer",
            "type": "python",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}